---
  - name: Import GrouPolicy Module
    ansible.windows.win_shell: |
      Import-Module -Name GroupPolicy

  - name: Add GPO
    ansible.windows.win_shell: |
      New-GPO -Name {{ gpo_name }} -Force

  - name: Run tests without check mode
    include_tasks: tests.yml